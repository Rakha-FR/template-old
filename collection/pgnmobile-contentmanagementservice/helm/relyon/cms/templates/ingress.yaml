{{- $ingressName := (printf "ingress%s" .Values.type ) -}}
{{- $ingress := index .Values $ingressName -}}
{{- if $ingress.enabled -}}
{{- $svcPort := .Values.service.targetPorthttp -}}
{{- $typeAPP := .Values.type -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cms-ingress-{{ .Values.type }}
  labels:
    app: cms-services-{{ .Values.type }}
  {{- with $ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingressall.ingressClassName }}
  {{- if $ingress.tls }}
  tls:
    {{- range $ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range $ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: doorv3-cms-services-{{ $typeAPP }}
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}