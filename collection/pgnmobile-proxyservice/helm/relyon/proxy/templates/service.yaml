# k8s service manifest
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: proxy-services
    app.kubernetes.io/managed-by: Helm
  name: doorv3-proxy-services
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: proxy-services
